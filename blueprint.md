# Lotto Number Generator

## Overview

A simple web application to generate lottery numbers.

## Features

*   **Lottery Number Generation:** Generates 6 unique random numbers between 1 and 45.
*   **User Interface:** A clean and simple interface to display the generated numbers.

## Current Plan

1.  **Modify `index.html`:**
    *   Update the title to "Lotto Number Generator".
    *   Add a main container for the application.
    *   Add a title heading.
    *   Add a container to display the lottery numbers.
    *   Add a button to trigger the number generation.
2.  **Modify `style.css`:**
    *   Add styles for the main container, title, number container, and button to create a visually appealing layout.
3.  **Modify `main.js`:**
    *   Implement the `generateNumbers` function to generate 6 unique random numbers.
    *   Add an event listener to the button to call the `generateNumbers` function and update the UI with the new numbers.
